/**
 * The AuthService (bcrypt + JWT based) has been replaced by better-auth.
 * Auth behaviours are tested via E2E tests against the running server.
 */

describe('AuthService', () => {
  it('placeholder — auth is handled by better-auth', () => {
    expect(true).toBe(true);
  });
});
