/**
 * Auth routes are now handled by better-auth at /api/auth/*.
 * The old custom auth routes have been removed.
 * Session-based auth is tested via integration tests against the running server.
 */

describe('Auth Routes', () => {
  it('placeholder — auth is handled by better-auth', () => {
    expect(true).toBe(true);
  });
});
